# Идеи по улучшению ArchLens

> Сводка: баги, доработки, большие задачи. Дополнять по мере обнаружения.

---

## Исправлено

- [x] **Компонент → Компонент** — добавлена связь `ui_component` → `ui_component` (depends_on) для композиции.
- [x] **Сайдбар: прозрачность и дизайн** — полупрозрачный фон (75%), backdrop-blur, скруглённые углы, деликатные границы.

---

## Баги и несоответствия

### Допустимые связи (allowedEdgeKinds)

- [ ] **ui_page → ui_page** — навигация/роутинг между страницами (depends_on)?
- [ ] **ui_page → state_store** — в PRD P04 есть правило "Direct UI → State" (warning, если страница напрямую к store без компонента). Сейчас связь запрещена — возможно, нужна для legacy-паттернов, но с issue.
- [ ] **database → cache** — read-through / cache-aside (сервис пишет в кэш, кэш читает из БД). Сейчас `database: {}` и `cache: {}` — из них нельзя вести связи. Может быть `service → cache`, `cache → database` для модели «кэш перед БД»?
- [ ] **cache → database** — если кэш промахивается, идёт запрос в БД.
- [ ] **api_gateway → database** — BFF-паттерн (Backend-for-Frontend), когда gateway напрямую ходит в БД. Сейчас нет.
- [ ] **external_system → service** — входящие webhook'и, callback'и. Сейчас `external_system: {}`.
- [ ] **Синхронизация allowedConnections и allowedEdgeKinds** — в `shared/model/config.ts` есть `allowedConnections`, в `editor/lib/utils.ts` — `allowedEdgeKinds`. Используется только allowedEdgeKinds. Либо удалить allowedConnections, либо привести к одному источнику правды.

### Редактор и UX

- [ ] **Тема MiniMap** — при переключении light/dark мини-карта не обновляет цвета.
- [ ] **Handle сверху** — верхняя точка подключения плохо видна.
- [ ] **Обводка нод** — слишком заметная при фокусе, хочется мягче.
- [ ] **Лишний скролл** — вертикальный скроллбар по умолчанию при ненужном overflow.

---

## Большая задача: переработка логики анализа

### Проблемы текущей механики

1. **Score-формула** — `100 - penalty + bonus` может дать >100. Бонусы за «нет циклов» завязаны на подстроку в `issue.title` — хрупко.
2. **Не все параметры учитываются** — `frequency`, `synchronous` на связях используются частично. `requestRate`, `hitRate`, `reliability` почти не влияют.
3. **Нет весов правил** — все issues одного severity штрафуются одинаково.
4. **Метрики без объяснений** — пользователь не понимает, откуда «Сложность frontend 8.0», «Нагрузка на API».
5. **Семантика связей** (TASK-071) — правила не проверяют осмысленность: reads/writes для БД, calls для сервисов, subscribes/emits для event-driven.

### Предложения

- [ ] **Документировать формулы** — явно описать расчёт score, grade, каждой метрики.
- [ ] **Вынести веса правил** — конфиг `ruleWeights: { S02: 20, L03: 15, ... }`.
- [ ] **Ограничить score 0–100** — `Math.max(0, Math.min(100, raw))`.
- [ ] **Tooltip к метрикам** — «Как считается», «Что значит».
- [ ] **Unit-тесты для правил** — фикстуры-графы, ожидаемые issues.
- [ ] **Правила семантики связей** — «reads/writes только для БД/кэша», «calls только для API/сервисов» → issues при нарушении.

---

## Функциональные улучшения

### Редактор

- [ ] **Undo/Redo** — Ctrl+Z / Ctrl+Y (частично есть в истории).
- [ ] **Multi-select** — выделение группы нод, перенос вместе.
- [ ] ~~Поиск по графу~~ — не требуется.
- [ ] **Drag-and-drop из сайдбара** — нода появляется рядом с курсором, не в случайном месте.
- [ ] **Zoom to fit / Center** — удобная навигация по большой схеме.

### Анализ

- [ ] **Приоритизация issues** — «Исправь первым», сортировка по impact.
- [ ] **Объяснение метрик** — «?» рядом с каждой метрикой.
- [ ] **Версия правил** — `rulesVersion` в ответе для совместимости.
- [ ] **AI-рекомендации** — уже есть (Groq), можно улучшить промпт/формат.

### Контент

- [ ] **Шаблоны** — «E-commerce», «Блог», «Микросервисы» для быстрого старта (частично есть пресеты).
- [ ] **Рекомендации по issue** — поле `recommendation` уже есть, можно усилить генерацию.

---

## UX и доступность

- [ ] **Горячие клавиши** — частично есть (Delete, Ctrl+Z/Y и т.д.).
- [ ] **Подсказки** — tooltips на критичных полях (уже частично есть).
- [ ] **Мобильная поддержка** — canvas на телефоне почти неюзабелен.
- [ ] **Accessibility** — focus, aria, навигация с клавиатуры.

---

## Инфраструктура

- [ ] ~~Unit-тесты для правил~~ — не в приоритете.
- [ ] **E2E-тесты** — критические сценарии (уже есть Playwright).
- [ ] **Версионирование схемы** — при смене правил старые результаты могут стать некорректными.

---

## Приоритеты

| Приоритет | Задачи |
|-----------|--------|
| **Высокий** | Переработка allowedEdgeKinds (все логичные связи), TASK-071 семантика связей, документирование формул score |
| **Средний** | Unit-тесты правил, объяснение метрик, приоритизация issues |
| **Низкий** | Мобильная поддержка, режим презентации, сохранение в облако |
