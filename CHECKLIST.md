# Чеклист: баги и доработки ArchLens

## Баги

- [x] **Tема MiniMap не обновляется по клику на toggle** — при переключении светлой/тёмной темы мини-карта не меняет цвета до перезагрузки страницы *(key={theme} для принудительного remount)*
- [x] **Верхняя точка (handle) на нодах не видна** — handles видны при hover на ноду
- [x] **Обводка нод слишком заметная** — в светлой и тёмной теме менее заметная обводка и ring при выделении
- [x] **Лишний вертикальный скролл справа** — по умолчанию появляется скроллбар *(html, body, #root: h-full overflow-hidden)*

## Доработки дизайна

- [x] **Панель редактора связи** — редизайн: иконка, секции, компактный footer
- [x] **Контекстное меню для связей** — ПКМ по связи: Редактировать, Удалить *(есть onEdgeContextMenu)*

## Будущие фичи

- [x] **Drag-and-drop из сайдбара** — перетаскивание на полотно, элемент появляется рядом с курсором *(use-canvas-dnd, sidebar draggable)*
- [x] **AI-агент** — интеграция AI для подсказок/рекомендаций *(Groq, AI-рекомендации на странице анализа)*
- [x] **Счётчик элементов и связей в редакторе** — CanvasStatsPanel в левом нижнем углу

---

## Улучшение анализа

- [ ] **Больше метрик** — добавить дополнительные метрики в результаты анализа
- [ ] **Пересмотр механизма оценки** *(большая задача)*:
  - Разобрать текущие формулы расчёта score/grade
  - Проанализировать параметры, которые передаются в элементах и связях
  - Сейчас порой «фигня» получает нормальную оценку — нужна более строгая и осмысленная логика
  - Углубить анализ в целом

---

## Валидация и ограничения

- [ ] **Допустимые связи** — продумать, какие связи между типами узлов возможны
- [ ] **Запрет недопустимых связей на клиенте** — блокировать совсем невозможные связи (например, frontend → database напрямую) до отправки на анализ

---

## UX и подсказки

- [x] **Больше подсказок для пользователя** — tooltips на полях нод/связей, METRIC_TOOLTIPS в результатах анализа

---

## Кастомные названия элементов

- [x] **Редактирование названия элемента** — displayName в свойствах, double-click по ноде *(базовое)*
- [ ] **Улучшения UX** *(на обдумать)*: badge типа, overlay при hover, subtitle — чтобы явно видеть тип при кастомном названии

---

## Критика и слабые зоны

### Анализ и оценка
- **Score-механика хрупкая** — бонусы добавляются к 100, штрафы вычитаются. `100 - penalty + bonus` может дать >100 или странные результаты. Бонусы за «нет циклов» определяются по подстроке в `issue.title` — при смене формулировок сломается.
- **Не все параметры учитываются** — `frequency`, `synchronous` на связях частично используются. Многие параметры нод (requestRate, hitRate, reliability) почти не влияют на правила.
- **Нет весов правил** — все issues одного severity штрафуются одинаково, хотя важность правил разная.
- **Метрики не объясняются** — откуда «Сложность frontend 8.0», как считается «Нагрузка на API» — пользователь не понимает.

### Редактор
- **allowedEdgeKinds неполный** — `database` и `cache` с пустыми `{}`, т.е. «из них нельзя вести связи». При этом импорт JSON с любой связью проходит — клиент не блокирует недопустимые графы.
- ~~Undo/Redo~~ — есть (Ctrl+Z/Y)
- ~~Multi-select~~ — есть (SelectionMode.Partial, рамка выделения)
- ~~Drag-and-drop из сайдбара~~ — есть (элемент под курсором)
- ~~Поиск по графу~~ — есть (GraphSearch)
- ~~Экспорт в изображение~~ — есть (PNG/SVG в меню Файл)

### UX и доступность
- **Слабая мобильная поддержка** — canvas-редактор на телефоне почти неюзабелен.
- ~~Горячие клавиши~~ — есть (Delete, Escape, Ctrl+Z/Y, Ctrl+X/C/V)
- **Accessibility** — focus management, aria-атрибуты, навигация с клавиатуры для canvas.

### Инфраструктура
- **Нет unit-тестов** на правила анализа — при изменении формул легко сломать логику.
- **Версионирование схемы** — при смене правил старые результаты могут стать некорректными.
- **Один пользователь** — нет совместного редактирования, истории изменений.

### Контент
- ~~Issues без приоритизации~~ — есть (SEVERITY_ORDER, фильтры по severity/category)
- ~~Нет шаблонов~~ — есть (CRUD, Микросервисы, Event-driven в sidebar)
- **Рекомендации общие** — мало конкретики «сделай X вместо Y».

---

## Дополнительные предложения по улучшению

### Приоритет: высокий
- [x] **Undo/Redo** — история действий в редакторе *(есть в store, Ctrl+Z/Y)*
- [x] **Горячие клавиши** — Delete, Escape, Ctrl+Z/Y, Ctrl+X/C/V *(use-editor-hotkeys)*
- [x] **Выделение группы элементов** — multi-select (SelectionMode.Partial, рамка)
- [x] **Экспорт в PNG/SVG** — меню Файл → Экспорт PNG/SVG
- [x] **Объяснение метрик** — METRIC_TOOLTIPS в analysis-metrics
- [x] **Приоритизация issues** — SEVERITY_ORDER, фильтры по severity/category

### Приоритет: средний
- [ ] **Корневой элемент «Система»** *(большая задача)*:
  - Новый тип узла `system` как корень приложения
  - Страницы (ui_page) наследуются/связаны с корневым элементом
  - У корня свои опции (название системы, окружение, масштаб и т.п.)
  - Один такой элемент на схеме — валидация «уже есть корень»
- [x] **Шаблоны архитектур** — пресеты CRUD, Микросервисы, Event-driven *(presets.ts)*
- [x] **Поиск по графу** — GraphSearch по названию/типу, подсветка
- [ ] **Zoom to fit / Center** — удобная навигация по большим схемам (частично есть через React Flow)
- [ ] **Проверка при импорте** — валидация связей по allowedEdgeKinds, предупреждения о «невозможных» ребрах
- [ ] **Unit-тесты для правил** — каждый rule — отдельный тест с fixture-графом
- [ ] **Версия правил в ответе** — `rulesVersion` в AnalysisResult для совместимости со старыми клиентами

### Приоритет: низкий
- [x] **Режим презентации** — полноэкранный просмотр схемы без UI *(TASK-072)*
- [ ] **Сохранение в облако** — не только localStorage (опционально, с бэкендом)
- [ ] **Сравнение версий** — diff двух схем / двух анализов
- [x] **Обучающий режим** — пошаговые подсказки *(use-tutorial, TutorialBanner)*
- [ ] **Тёмная тема для страницы приветствия** — если / пустая или redirect

---

*Добавляйте новые пункты по мере обнаружения.*
в будущем добавить настройки узлов и связей в сайдбар справа